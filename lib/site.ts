// Single source of truth for the canonical site URL.
// Used by sitemap, robots, metadata (canonical/hreflang/OpenGraph).
// If the domain ever changes, update it here — nowhere else.
export const SITE_URL = 'https://namtt.dev';
